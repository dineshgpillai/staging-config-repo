spring:
  cloud:
    consul:
      host: localhost
      port: 8500
      enabled: false
      discovery:
        metadata:
            myfield: myvalue
            anotherfield: anothervalue
        instanceId: ${spring.application.name}:${random.value}



application:
  value1: 'This is good INT'